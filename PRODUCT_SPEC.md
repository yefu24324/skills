# Product Skills 的目标与用途

本文记录 `product-spec`、`product-spec-author` 和 `product-spec-review` 三个 Skill 背后的人类目标，作为后续设计、修改和评审这些 Skill 时的意图基线。

原始讨论：[需求文档重构与优化](https://chatgpt.com/share/6a718a3b-78b0-83e8-b393-9d637751fd86)

## 核心目标

将这组 Skill 定位为 **AI 产品架构师**，而不是普通的 PRD 文本生成器。

它们应帮助人类持续维护一个清晰、正确、当前有效的产品模型，把业务目标、用户角色、产品能力、业务对象、流程、页面、功能板块、交互状态、业务规则和验收标准组织成一致的产品规格。

最终产物既要表达“为什么做、做什么”，也要表达功能如何落到页面和交互结构中，使产品、设计、开发和测试能够基于同一份当前规格继续工作。

## 要解决的问题

### 1. 避免需求补丁不断堆积

需求发生变化时，不能默认只在旧文档后面追加补充说明。

Skill 应先判断变化是否已经影响原有产品结构。局部变化可以修改；角色、流程、权限、对象生命周期、页面职责或能力边界发生较大变化时，应重新组织并完整重写受影响的当前需求，删除已经失效的描述。

产品文档应始终优先表达当前真实需求，而不是把历史修改过程混在当前规格中。

### 2. 保持文档对人类友好

不能让所有需求持续堆在一个过长文件中。应按产品职责和阅读目的合理拆分页面、流程、对象和共享规则，使每份文档可以独立理解，又能通过稳定关系追踪到完整产品。

拆分的目的不是追求固定行数，而是降低理解和维护成本。

### 3. 让需求包含可理解的页面结构

产品规格不仅要列出功能，还要说明：

- 一个功能涉及哪些页面；
- 每个页面包含哪些功能板块；
- 板块之间大致的顺序、并列、嵌套和更新关系；
- 主要操作位于哪个功能区域；
- 重要页面状态和特殊交互如何影响页面或业务流程。

页面结构优先使用 ASCII 图表达。ASCII 的目标是清楚描述信息结构和功能区域，不是制作像素级线框图。ASCII 难以表达的特殊交互，可以用简短文字说明。

### 4. 通过独立审阅提高规格质量

产品阶段不应由同一个 Agent 编写后自行宣布完成。应形成以下协作过程：

```text
调查现状
  -> 发现高影响歧义时与人类沟通
  -> Author 编写或重构 PRD
  -> 独立 Reviewer 审阅
  -> 打回 Author 修改，或批准进入下一阶段
```

Reviewer 应检查当前规格是否清晰、完整、一致、可实现和可测试，并识别补丁堆积、结构错误、状态缺失、规则冲突和文档拆分问题。Reviewer 负责指出问题和给出审阅结论，不应一边审阅一边替 Author 修改被审文档。

### 5. 将无法推导的产品决策交还人类

Agent 应先从已有文档、代码、测试和其他项目证据中调查事实，不要把可以检索到的问题重新询问人类。

当不明确内容会影响产品目标、范围、角色、权限、主流程、对象生命周期、页面职责、业务规则、删除行为、资金、合规、外部承诺或验收结果时，应优先与人类沟通并等待决定，然后再继续编写需求。Agent 不应把自己的产品偏好伪装成已经确认的需求。

## 三个 Skill 的用途

### `product-spec`

作为 AI 产品架构师入口和产品阶段协调者使用：

- 调查项目现状并建立当前产品模型；
- 判断请求属于新建、修改还是审查；
- 识别变更影响以及应局部修改还是重写；
- 管理人类澄清门禁；
- 协调 Author 编写和 Reviewer 审阅；
- 维护功能、流程、页面、规则和验收之间的一致性；
- 在独立审阅通过前，不把产品阶段声明为完成。

### `product-spec-author`

用于创建或重构产品规格：

- 将模糊需求整理成明确、可追踪的当前规格；
- 编写产品能力、对象、流程、页面、板块、状态、规则和验收标准；
- 为用户可见页面提供高质量 ASCII 页面结构；
- 将过长或职责混杂的 PRD 拆成模块化文档；
- 在重大需求变化后重写受影响内容，而不是继续追加补丁；
- 根据 Reviewer 的打回意见生成新的修订版本。

Author 可以编写规格，但不能批准自己的产物。

### `product-spec-review`

用于独立审查已有产品规格：

- 检查目标、范围、角色、权限、对象、流程、页面、规则、状态和验收是否完整一致；
- 检查页面结构是否足以让人理解主要功能区域和关系；
- 识别新旧需求并存、补丁链、失效引用和共享规则冲突；
- 检查文档是否过度集中或过度拆分；
- 区分 Author 可以修复的问题和必须由人类决定的问题；
- 给出批准、打回修改或请求人类决策的明确结论。

Reviewer 保持独立，只审阅指定版本，不直接重写被审 PRD。

## 典型使用场景

- 从零梳理一个新产品或新功能的产品规格；
- 把模糊想法整理成可供设计、开发和测试使用的需求；
- 描述一个功能涉及的页面、页面板块及其大致结构；
- 在需求发生重大变化后重新构建相关产品模型和文档结构；
- 清理长期追加形成的补丁式 PRD；
- 将对人类不友好的超长需求文档拆成职责清晰的模块；
- 独立审查已有 PRD 是否完整、一致和可执行；
- 在产品阶段通过 Agent 编写、独立审阅和人类决策形成质量门禁。

## 期望产出

这组 Skill 产出的产品规格应当：

- 描述当前有效需求，不依赖已废弃内容；
- 简单清晰，对人类阅读和维护友好；
- 将目标、流程、页面、板块、规则、状态和验收相互关联；
- 详细到足以支持后续设计、开发、测试和审阅；
- 明确区分事实、推断、假设和待人类决定的问题；
- 经独立 Reviewer 审查后才视为产品阶段完成。

## 明确非目标

- 不把产品规格写成颜色、字体、字号、像素尺寸、间距等视觉设计稿；
- 不要求 ASCII 页面结构达到像素级精度；
- 不用一个不断增长的单体文件保存所有需求和历史；
- 不把重大需求变化处理成旧文档末尾的新补丁；
- 不允许 Agent 擅自决定高影响、无法从证据推导的产品取舍；
- 不允许 Author 通过自我审阅批准自己的产品规格。
