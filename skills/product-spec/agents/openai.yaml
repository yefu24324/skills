interface:
  display_name: "Product Spec Orchestrator"
  short_description: "Clarify, author, review, and approve PRDs"
  default_prompt: "Use $product-spec to run the product stage: investigate first, ask humans about blocking ambiguities, dispatch an author to write the PRD, then require an independent reviewer verdict and rework loop before approval."
